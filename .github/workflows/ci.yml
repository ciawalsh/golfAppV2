name: CI

on:
  pull_request:
    branches: [main]

jobs:
  lint-and-typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: TypeScript check
        run: npx tsc --noEmit

      - name: ESLint
        run: npm run lint

      - name: Prettier
        run: npm run format

      - name: Secret scan
        run: |
          FOUND=$(grep -rn "AIza\|sk_live\|sk_test\|Bearer \|apiKey.*=.*['\"]" src/ functions/src/ app/ --include="*.ts" --include="*.tsx" || true)
          if [ -n "$FOUND" ]; then
            echo "::error::Potential secrets found in source code:"
            echo "$FOUND"
            exit 1
          fi
          echo "No secrets found."
